{
  "name": "led-kitchen-webapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate-cert": "mkdir -p certs && openssl req -x509 -newkey rsa:2048 -keyout certs/key.pem -out certs/cert.pem -days 365 -nodes -subj '/CN=localhost'",
    "start": "serve --ssl-cert certs/cert.pem --ssl-key certs/key.pem -l 3443"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
